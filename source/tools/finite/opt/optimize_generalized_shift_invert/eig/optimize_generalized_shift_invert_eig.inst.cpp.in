#include "optimize_generalized_shift_invert_eig.impl.h"

using Scalar = @SCALAR@;

/* clang-format off */
template opt_mps<Scalar> tools::finite::opt::internal::optimize_generalized_shift_invert_eig(const TensorsFinite<Scalar> &tensors,
                                                                                              const opt_mps<Scalar> &initial_mps, OptMeta &meta,
                                                                                              reports::eigs_log<Scalar> &elog);
/* clang-format on */
