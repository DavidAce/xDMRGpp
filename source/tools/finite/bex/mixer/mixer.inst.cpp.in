#include "mixer.impl.h"

using T = @SCALAR@;

template void tools::finite::bex::internal::run_expansion_term_mixer(TensorsFinite<T> &tensors, size_t posP, size_t pos0, const BondExpansionConfig &bcfg);
